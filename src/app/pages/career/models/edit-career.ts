import { Data } from "@angular/router";

export interface EditCareer {
  //lo que piense que se debe iditar en mi base de datos
  name:                      string,
  dateStart:                 string,
  dateEnd:                   string,
  timeRenovationAgreement:   string,
  coordinator:               string,
  viceCoordinator:           string,
  respStepDual :             string,


}
